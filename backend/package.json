{
  "name": "heroku-build",
  "version": "1.0.0",
  "description": "Build script for Heroku",
  "scripts": {
    "heroku-postbuild": "cd ../frontend && npm install && npm run build && cd ../backend && python manage.py collectstatic --noinput"
  }
}


